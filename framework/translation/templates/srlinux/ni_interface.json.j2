{
  "path": "/network-instance[name={{ network_instance }}]",
   "value": {
      "name": "{{ network_instance }}",
      "admin-state": "enable",
      "interface": [
       {
        "name": "{{ name ~ '.' ~ subinterface }}"
       }
        ]
    }
}